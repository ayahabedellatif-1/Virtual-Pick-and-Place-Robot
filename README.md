# Virtual-Pick-and-Place-Robot
 SimSort is a virtual pick-and-place robot that uses a webcam to detect and track objects, simulates robotic arm movement with forward and inverse kinematics, and automatically sorts objects into virtual bins. It recreates an industrial sorting workflow without requiring physical robot hardware.
